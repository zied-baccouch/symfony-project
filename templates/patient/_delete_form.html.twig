<form method="post" action="{{ path('app_patient_delete', {'idPatient': patient.idPatient}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patient.idPatient) }}">
    <button class="btn">Delete</button>
</form>
